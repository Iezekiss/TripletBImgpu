# 🧾 README

## Комплексный аналитический проект: BI, ML, ETL, визуализация

### 1. Описание проекта

Проект включает полный цикл аналитики и визуализации данных:
— обработка и исследование датасета декоративной и уходовой косметики,
— разработка ABC / XYZ / RFM / когортного анализа,
— построение моделей в BigQuery ML,
— визуализация результатов в Power BI и Tableau,
— построение ETL-конвейера в Airflow,
— построение витрины данных и дашбордов в Superset.

Работа опирается на теоретические материалы и оформляется в соответствии с академическими требованиями (см. отчёт ).

---

## 2. Структура репозитория

```
project/
│
├── data/                   # Используемые датасеты
│   ├── makeup_data.json
│   ├── output.csv
│
├── LB4_Kuzmina_V11/        # Материалы курса, отчёты, PDF
│
├── dags/                   # DAG Airflow
│   ├── extract_kaggle.py
│   ├── load_postgres.py
│   ├── datamart.sql
│
├── img/                    # Скриншоты для отчётов
│
├── Tableau/                # Файлы Tableau
│   ├── Book1.twb
│   ├── Book1.twbx
│
├── docker-compose.yml      # Запуск Airflow / Superset
├── Dockerfile
├── superset.Dockerfile
│
├── README.md               # (этот файл)
└── requirements.txt        # Зависимости проекта
```

---

# 3. Датасет

Использован датасет декоративной косметики с платформы opendatabay:
*Формат:* JSON, ~900 записей.

Ключевые поля:

* `brand` — бренд
* `name` — наименование товара
* `price`, `price_sign`, `currency` — стоимость
* `product_type`, `category`
* `description`
* `product_colors[]`
* `image_link`

Датасет позволяет проводить EDA, ценовой анализ и построение ML-моделей.

---

# 4. Аналитический блок

## 4.1. ABC-анализ

**Цель:** разделение брендов по вкладу в суммарную стоимость ассортимента.

**Методика:**

1. Агрегирование цен по брендам
2. Сортировка
3. Расчёт накопленной доли
4. Классификация A/B/C

**Результаты:**

* **A-бренды:** Clinique, Dior, Smashbox, Nyx — формируют основную ценность каталога
* **B-бренды:** Maybelline, L’Oreal, Cargo
* **C-бренды:** множество малых брендов с минимальной представленностью

**Вывод:** рынок высококонцентрирован, сложность управления распылённым хвостом.

---

## 4.2. XYZ-анализ

**Цель:** оценка предсказуемости цен внутри бренда через коэффициент вариации (CV).

**Критерии:**

* X: CV < 0.10 — стабильные цены
* Y: 0.10–0.25 — умеренно стабильные
* Z: ≥0.25 — сильные скачки

**Результаты:**

* **X:** бренды с узкой линейкой и стабильными ценами (Piggy Paint, Alva…)
* **Y:** средняя вариативность
* **Z:** Nyx, Clinique, Dior — широкий ценовой диапазон

**Вывод:** категория Z демонстрирует наиболее разнородную ценовую структуру.

---

## 4.3. RFM-анализ (адаптация под каталог)

Транзакций нет, использованы допустимые адаптированные метрики:

| R (Richness) | Количество товаров бренда |
| F (Frequency) | Доля бренда в каталоге |
| M (Monetary) | Средняя цена |

**Сегменты:**

* **Премиальные лидеры:** Dior, Clinique
* **Масс-маркет с широкой матрицей:** Nyx, Maybelline
* **Бюджетные низкочастотные:** Essence, Avon

---

## 4.4. Когортный анализ (ассортиментный)

Когорты сформированы по числу товаров бренда:

* **A-когорта:** бренды-ядро с широким ассортиментом
* **B:** средняя представленность
* **C:** 1–2 товара, низкая значимость

**Вывод:** основная масса ассортимента лежит в A-когорте, «хвост» требует ревизии.

---

# 5. Моделирование в BigQuery ML

## 5.1. Цель

Построение модели для предсказания цены на основе признаков:
`brand`, `product_type`.

## 5.2. Подготовка таблиц

Создана очищенная таблица:

```sql
CREATE OR REPLACE TABLE makeup_clean AS
SELECT
  id,
  brand,
  product_type,
  SAFE_CAST(price AS FLOAT64) AS price
FROM makeup_raw
WHERE SAFE_CAST(price AS FLOAT64) IS NOT NULL;
```

## 5.3. Обучение модели

```sql
CREATE OR REPLACE MODEL price_model
OPTIONS(
  model_type='linear_reg',
  input_label_cols=['price'],
  data_split_method='AUTO_SPLIT'
) AS
SELECT brand, product_type, price
FROM makeup_clean;
```

## 5.4. Оценка качества

Метрики BigQuery ML:

* MAE ≈ 19
* MSE ≈ 7270
* R² = −49.35

**Интерпретация:**
Линейная модель не описывает данные → годится только как демонстрационный пример ML-пайплайна.

## 5.5. Прогнозирование

```sql
SELECT *
FROM ML.PREDICT(
  MODEL price_model,
  (SELECT 'colourpop' AS brand, 'lipstick' AS product_type)
);
```

---

# 6. BI: визуализация в Power BI и Tableau

## Power BI

Проведена визуализация:

* сводные показатели
* ABC/XYZ сегментация
* распределение цен
* анализ структуры брендов
* графики вариативности

## Tableau

Создан интерактивный дашборд:
— распределения по брендам,
— анализ цен,
— сегменты ассортимента,
— визуализация оттенков.

---

# 7. ETL-конвейер (Airflow)

Создан DAG из трёх этапов:

### 7.1. Extract

Загрузка данных с Kaggle через API:
— использование `kaggle` CLI
— сохранение в `/opt/airflow/dags/data/`

### 7.2. Load

Загрузка CSV в PostgreSQL через `PostgresHook`.

### 7.3. Transform

Создание витрины данных через PostgresOperator и SQL-файл:

```sql
CREATE OR REPLACE VIEW flights_mart AS
SELECT ...
```

Все шаги успешно протестированы в окружении Docker (Airflow + PostgreSQL + Superset).

---

# 8. Superset: дашборды

На основе витрины созданы визуализации:

* Pie Chart — структура по авиакомпаниям
* Bar Chart — доля задержек
* Line Chart — динамика средней задержки
* KPI — общая доля задержек
* Mixed Chart — количество рейсов vs задержки

Основные выводы:
— ярко выраженная сезонность,
— различия между авиакомпаниями,
— зависимость задержек от нагрузки и времени года.

---

# 9. Итоговые выводы проекта

1. Проведён комплексный цикл аналитики — от сырого JSON до финальных дашбордов.
2. Датасет косметики подходит для многомерной сегментации.
3. BigQuery ML успешно применён, модель обучена и протестирована.
4. Tableau и Power BI дали глубокую визуальную интерпретацию данных.
5. Построен рабочий ETL-конвейер для другого датасета (авиарейсы) на Airflow.
6. Сформирована витрина данных и дашборд Superset с расширенным функционалом.
7. Проект можно масштабировать: добавить новые признаки, использовать ML-алгоритмы Boosted Trees, расширить ETL.

